"""
BIT Dashboard

Local operator dashboard for monitoring the BIT trading bot.
Exposes real project state — no fake data.

Run:
    python -m bit.dashboard
or:
    uvicorn bit.dashboard.app:app --host 127.0.0.1 --port 8765
"""
